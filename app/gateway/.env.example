# Apollo Gateway Configuration
# Copy this file to .env and fill in your actual values

# Hasura Cloud Endpoint (from Phase 1)
# Get this from: https://cloud.hasura.io/ → Your Project → GraphQL Endpoint
# Example: https://your-project-name.hasura.app/v1/graphql
HASURA_GRAPHQL_ENDPOINT=https://your-project.hasura.app/v1/graphql

# Hasura Admin Secret (from Phase 1)
# Get this from: Hasura Cloud → Project Settings → Env Vars → HASURA_GRAPHQL_ADMIN_SECRET
HASURA_GRAPHQL_ADMIN_SECRET=your-admin-secret-here

# Providers Subgraph URL
# Local development: http://localhost:3002/graphql
# Production (Render.com): https://your-providers-subgraph.onrender.com/graphql
SUBGRAPH_URL=http://localhost:3002/graphql

# Gateway Port
GATEWAY_PORT=4000

# Apollo GraphOS (Optional - for managed federation)
# Uncomment these when using managed federation in production
# Get these from: Apollo Studio → Your Graph → Settings → API Keys
# APOLLO_KEY=service:claimsight-api:your-graph-api-key
# APOLLO_GRAPH_REF=claimsight-api@current
