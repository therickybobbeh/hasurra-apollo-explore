# Hasura Cloud Environment Variables
# Copy this file to .env.cloud and fill in your actual values
# .env.cloud is gitignored and won't be committed

# Hasura Cloud GraphQL Endpoint
# Get from: Hasura Cloud dashboard
# Format: https://your-project-name.hasura.app/v1/graphql
HASURA_GRAPHQL_ENDPOINT=https://your-project-name.hasura.app/v1/graphql

# Hasura Admin Secret
# Get from: Hasura Console → Project Settings → Env Vars → HASURA_GRAPHQL_ADMIN_SECRET
HASURA_GRAPHQL_ADMIN_SECRET=your-admin-secret-here

# Neon PostgreSQL Database URL (Optional - for direct database access)
# Get from: Hasura Console → Data tab → Database → Connection Settings
# Format: postgres://user:password@ep-xxxxx.region.aws.neon.tech/dbname?sslmode=require
DATABASE_URL=postgres://user:password@ep-xxxxx.us-east-2.aws.neon.tech/neondb?sslmode=require

# Optional: Apollo GraphOS credentials (for Step 7 - Apollo deployment)
# APOLLO_KEY=service:claimsight-api:your-key-here
# APOLLO_GRAPH_REF=claimsight-api@main
